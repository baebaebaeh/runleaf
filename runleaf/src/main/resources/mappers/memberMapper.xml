<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="kr.kro.runleaf.member.repository.MemberRepository">
	<!-- 회원가입 -->
	<insert id="insertMember" parameterType="MemberDto">
		INSERT INTO member(
		username, password, nickname, email, phone, profile_image
		) VALUES (
		#{username}, #{password}, #{nickname}, #{email}, #{phone}, #{profileImage}
		)
	</insert>
	<!-- 회원조회 -->
	<select id="selectMember" parameterType="int" resultType="kr.kro.runleaf.member.domain.Member">
		SELECT nickname, email, phone, profileImage
		FROM member
		WHERE id = #{id}
	</select>
	<!-- 회원수정 -->
	<update id="updateMember" parameterType="MemberDto">
		UPDATE member
		SET
		username = #{username},
		password = #{password},
		email = #{email},
		phone = #{phone},
		profile_image = #{profileImage}
		WHERE id = #{id}
	</update>
	<!-- 회원삭제 -->
	<delete id="deleteMember" parameterType="int">
		DELETE FROM book
		WHERE id = #{id}
	</delete>	
</mapper>
