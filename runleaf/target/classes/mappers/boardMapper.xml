<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.kro.runleaf.board.repository.BoardDao">
	<!-- 게시물 목록 데이터 -->
	<select id="selectBoardByDistance" resultType="Board" parameterType="BoardSearch">
		select *
		  from board
		 order by no desc
		 limit #{offset}, #{listSize}
	</select>
	
	<insert id="insertBoard" parameterType="Board" useGeneratedKeys="true" keyProperty="no">
		<!-- 
    	<selectKey keyProperty="no" resultType="int" order="AFTER">
    		select last_insert_id() from dual
    	</selectKey>
		 -->
    	insert into board (title, writer, content)
	    values (#{title}, #{writer}, #{content})
	</insert>	

</mapper>



